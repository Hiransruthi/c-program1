// Online C compiler to run C program online
#include <stdio.h>

int main() {
    int a,b,c,lcm;
    printf("enter the numbers: ");
    scanf("%d %d %d",&a,&b,&c);
    for(lcm=1;lcm<=(a*b*c);lcm++)
    if(lcm%a==0 && lcm%b==0 && lcm%c==0)
    printf("the lcm is %d",lcm);
    return 0;
}
