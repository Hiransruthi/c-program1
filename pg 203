#include <stdio.h>

int main()
{
    int num;
    int *ptr;

    printf("Enter a number: ");
    scanf("%d", &num);

    printf("Original number: %d\n", num);

    ptr = &num;

    // Change the value using pointer
    *ptr = *ptr + 10;

    printf("Changed number: %d\n", num);

    return 0;
}
