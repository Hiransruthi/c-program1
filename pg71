// Online C compiler to run C program online
#include <stdio.h>

int main() {
    int x,a,b,c,d;
      //printf("enter a number:");
      //scanf("%d",&a);
    for(x=10;x<100;x++)
    {
        if (x%2!=0)
          {
            b=x%10;
            c=x/10;
            if (b+c==7)
            printf("%d\n",x);
          }
          
    }
    return 0;
}
