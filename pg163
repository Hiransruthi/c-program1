// Online C compiler to run C program online
#include <stdio.h>
#include <math.h>

int check_last_digit_odd();
int main()
{
    int x;
    x=check_last_digit_odd();
    printf("the required numebr  is %d",x);
    return 0;
}
int check_last_digit_odd()
{
    int c=0,a,b,sum=0,d,ip,e,f,g,h;
    printf("enter the number: ");
    scanf("%d",&ip);
    a=ip;
    while(a>0)
    {
        b=a%10;
        c=c+1;
        a=a/10;
    }
    a=ip;
    d=a/pow(10,(c-1));
    if(d%2!=0)
    {
        d=d-1;
        f=d*pow(10,(c-1));
        g=a%(int)pow(10,(c-1));
        h=f+g;
        return (h);
    }
    else
    return (a);
}
