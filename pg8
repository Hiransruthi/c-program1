// Online C compiler to run C program online
#include <stdio.h>

int main() {
    int x;
    printf("enter the 3 digit number: ");
    scanf("%d",&x);
    x=x/10;
    printf("answer: %d",x/10);
    return 0;
}
