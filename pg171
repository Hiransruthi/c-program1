// Online C compiler to run C program online
#include <stdio.h>
#include <math.h>

int disp_count_sum14();
int main()
{
    int x;
    x=disp_count_sum14();
    printf("count = %d",x);
    return 0;
}
int disp_count_sum14()
{
    int a,c=0,b,i,sum,d,temp;
    //while(a>0)
    for(a=10;a<=9999;a++)
    {
        temp=a;
        sum=0;
        while(temp>0)
        {
            d=temp%10;
            sum=sum+d;
            temp=temp/10;
        }
        if (sum==14)
         {
           c=c+1;
         }
    }
    return (c);
}
