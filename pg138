// Online C compiler to run C program online
#include <stdio.h>
#include <math.h>
int check_last_digit_odd();
int main() 
{
    check_last_digit_odd();
    return(0);
}
int check_last_digit_odd()
{
    int a,c=0,b,d,e,f,ip,x,g,h;
    printf("enter the number: ");
    scanf("%d",&ip);
    x=ip;
    while(x>0)
    {
        a=x%10;
        c=c+1;
        x=x/10;
    }
    //printf("%d\n",c);
   x=ip;
   d=x%(int)pow(10,(c-1));
   b=x/pow(10,(c-1));
   if(b%2==0)
   {
       printf("%d",x);
   }
   else
   {
       b=(b-1)*pow(10,(c-1));
       g=b+d;
       printf("%d",g);
   }
    return(0);
}

  
