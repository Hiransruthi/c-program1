#include <stdio.h>

int main()
{
    int n, i, flag, count = 0,c=0,a,ip,b;
    printf("enter the number: ");
    scanf("%d",&ip);
    n=ip;
    while(n>0)
    {
        a=n%10;
        c=c+1;
        n=n/10;
    }
    //printf("the count : %d \n",c);
    n=ip;
    for(i=1;i<c;i++)
    {
        b=n%100;
        if(b%2!=0)
        count++;
        n=n/10;
    }
    printf("%d\n",count);

    return 0;
}
