// Online C compiler to run C program online
#include <stdio.h>

int main() {
     int x,s=0;
     x=1;
    loop:if(x<6)
   { s=s+x;
     x=x+1;
     goto loop;
   }
     printf(" %d",s);
       return 0;
}  
