// Online C compiler to run C program online
#include <stdio.h>

int main() {
    int x,sum=0;
    for (x=10;x<100;x++)
    {
        if ((x%2)!=0)
        {
            if(x/10==7)
            sum=sum+x;
        }
        
    }
    printf("%d",sum);

    return 0;
}
