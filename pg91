#include<stdio.h>

int main() {
    int x,sum,count=0,i,temp;
    for(x=1;x<100000;x++)
    {
        x= temp ;
        sum=0;
        while(x>0)
          {
            i=temp%10;
            sum=sum+i;
            temp=temp/10;
          }
       if(sum==14)
         {
           count++;
         }
    }
    printf("answer %d",count);
    return 0;
}
