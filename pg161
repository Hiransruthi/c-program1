// Online C compiler to run C program online
#include <stdio.h>

int disp_reverse_number();
int main()
{
    int x;
    x=disp_reverse_number();
    printf("the reverse numebr  is %d",x);
    return 0;
}
int disp_reverse_number()
{
    int c=0,a,b,sum=0,d,ip,e,f;
    printf("enter the number: ");
    scanf("%d",&ip);
    a=ip;
    while(a>0)
    {
        b=a%10;
        c=c+1;
        a=a/10;
    }
    a=ip;
    for(d=1;d<=c;d++)
    {
       e=a%10;
       f=f*10+e;
       a=a/10;
    }
return (f);
}
//printf("%d is the sum",sum);
