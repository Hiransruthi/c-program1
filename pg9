// Online C compiler to run C program online
#include <stdio.h>

int main() {
    int x,y,z,a;
    printf("enter the 3 digit number: ");
    scanf("%d",&x);
    a=x%10;
    y=a%10;
    z=a/10;
    printf("sum: %d",a+y+z);
    return 0;
}
