// Online C compiler to run C program online
#include <stdio.h>

int main() {
    int x,a,sum=0;
      //printf("enter a number:");
      //scanf("%d",&a);
    a=5;
    for(x=1;x<=a;x++)
    {
        sum=sum+x;
      }
     printf(" %d\n",sum);

    return 0;
}
