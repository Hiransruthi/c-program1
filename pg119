#include <stdio.h>
int main()
  {   
      int a,b,count=0;
      a=9999;
      while(a>0)
      {
        if(a%7==0 && a%9==0)
        {
            printf("the number is : %d",a);
            break;
        }
          a=a-1;
      }
    
    return 0;
  }
