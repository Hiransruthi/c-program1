#include <stdio.h>
int main()
{ 
    int x,sum=0,a;
    printf("enter the number: ");
    scanf("%d",&x);
    while(x>0)
    {
      if(x/10>=0)
      a=x%10;
      sum=sum+a;
      x=x/10;
    }
    printf("sum of digits is : %d",sum);
    return 0;
}
