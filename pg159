// Online C compiler to run C program online
#include <stdio.h>

int count_total_digits();
int main()
{
    int x;
    x=count_total_digits();
    printf("the count is %d",x);
    return 0;
}
int count_total_digits()
{
    int c=0,a,b;
    printf("enter the number: ");
    scanf("%d",&a);
    while(a>0)
    {
        b=a%10;
        c=c+1;
        a=a/10;
    }
    return (c);
}
//printf("%d is the sum",sum);
