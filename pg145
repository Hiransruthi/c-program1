#include <stdio.h>

int disp_total_2digit_odd();

int main()
{
    //int ip;
    //printf("Enter a number: ");
    //scanf("%d", &ip);

    disp_total_2digit_odd();

    return 0;
}

int disp_total_2digit_odd()
{
    int i,a,c=0;
    printf("enter the number : ");
    scanf("%d",&i);
    while(i>=10)
    {
        a=i%100;
        if(a%2!=0)
        {
            c=c+1;
        }
        i=i/10;
    }
    printf("%d",c);
    return 0;
}
