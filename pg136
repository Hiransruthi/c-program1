// Online C compiler to run C program online
#include <stdio.h>
int disp_reverse_number();
int main() 
{
    disp_reverse_number();
    return(0);
}
int disp_reverse_number()
{
    int a,b=0,c=0,x,y,ip,i;
    printf("enter the number: ");
    scanf("%d",&ip);
    x=ip;
    while(x>0)
    {
        y=x%10;
        c=c+1;
        x=x/10;
    }
    x=ip;
    while(x>0)
    {
    for(i=1;i<=c;i++)
    {
        a=x%10;
        b=b*10+a;
        x=x/10;
    }
    }
    printf("%d",b);
    return(0);
}

  
