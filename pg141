#include <stdio.h>

int check_prime();

int main()
{
    //int ip;
    //printf("Enter a number: ");
    //scanf("%d", &ip);

    check_prime();

    return 0;
}

int check_prime()
{
    int i, flag = 0,ip,sum=0,b,x,a;
    printf("enter the number :");
    scanf("%d",&a);
    ip=a%100;
    x=a;
    if (ip <= 1)
    {
        printf("%d is not a prime number", ip);
        return 0;
    }

    for (i = 2; i < ip; i++)
    {
        if (ip % i == 0)
        {
            flag = 1;
            break;
        }
    }
    if(flag==0)
    printf("prime");
    else
    printf("not prime");
    return 0;
}
