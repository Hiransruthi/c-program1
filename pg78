// Online C compiler to run C program online
#include <stdio.h>

int main() {
    int x,a=0,c=0,ip,i,b,d;
    printf("enter the number:");
    scanf("%d",&ip);
    x=ip;
    while(x>0)
    {
        x=x/10;
        c=c+1;
    }
    printf("%d\n",c);
    x=ip;
    for(i=1;i<=c;i++)
    {
        a=x%10;
        b=a+b*10;
        x=x/10;
    }
   
        printf("the rev number is: %d",b+x);
    return 0;
}
