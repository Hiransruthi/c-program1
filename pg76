// Online C compiler to run C program online
#include <stdio.h>

int main() {
    int x,c=0;
    printf("enter the number:");
    scanf("%d",&x);
    while(x>0)
    {
        x=x/10;
        c=c+1;
    }
    printf("%d",c);

    return 0;
}
