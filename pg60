// Online C compiler to run C program online
#include <stdio.h>

int main() {
    int n,x=0,a,x1;
    printf("enter the num :");
    scanf("%d",&n);
    while (n/10>0)
    {
      a=n%10;
      x=(a+x)*10;
      n=n/10;
      x1=x+n;
    }
     printf("the reversed num is: %d",x1);
    return 0;
}
