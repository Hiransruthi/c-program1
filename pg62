// Online C compiler to run C program online
#include <stdio.h>
#include <math.h>
int main() {
    int a,b,c=0,d,e,f,g,h,i,j,ip;
 printf("enter a number:");
 scanf("%d",&a);
 while (a>0)
 {  
     a=a/10;
     c=c+1;
 }
  printf("%d\n",c);
printf("enter the num again: ");
scanf("%d",&ip);
d=ip%10;//ones digit
d=d*pow(10,(c-1));
printf("%d\n",d);
e=ip%(int)pow(10,(c-1));
f=e/10;
g=f*10;//middle
printf("%d\n",g);
h=ip/pow(10,(c-1));//first 
printf("%d\n",h);
i=d+g+h;
printf("the req num is : %d",i);

   return 0;
  }
