// Online C compiler to run C program online
#include <stdio.h>

int main() {
    int x,a,b,c,d;
    printf("enter the 2 digit number: ");
    scanf("%d",&x);
    a=x%10;
    b=a*10;
    c=x/10;
    d=c*1;
    printf("reversed: %d",b+d);
    return 0;
}
