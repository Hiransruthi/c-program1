// Online C compiler to run C program online
#include <stdio.h>

int main() {
     int x;
     x=5;
    loop:if(x<6&& x>0)
   { 
     printf("%d\n",x);
     x=x-1;
     goto loop;
   }
    return 0;
}  
