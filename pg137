// Online C compiler to run C program online
#include <stdio.h>
#include <math.h>
int disp_reverse_number();
int main() 
{
    disp_reverse_number();
    return(0);
}
int disp_reverse_number()
{
    int a,c=0,b,d,e,f,ip,x,g,h;
    printf("enter the number: ");
    scanf("%d",&ip);
    x=ip;
    while(x>0)
    {
        a=x%10;
        c=c+1;
        x=x/10;
    }
    printf("%d\n",c);
    x=ip;
       {
           b=x%10;
           d=b*pow(10,(c-1));
           e=x/pow(10,(c-1));
           f=x/10;
           g=(f%(int)pow(10,(c-2)))*10;
       }
        h=d+g+e;
        printf("%d",h);
        
    return(0);
}

  
