#include <stdio.h>
int main()
{ 
    int x,count=0;
    printf("enter the number: ");
    scanf("%d",&x);
    while(x>0)
    {
      if(x/10>=0)
      count++;
      x=x/10;
    }
    printf("number of digits is : %d",count);
    return 0;
}
