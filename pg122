// Online C compiler to run C program online
#include <stdio.h>

int main() {
    int a,b,c,hcf;
    printf("enter the numbers: ");
    scanf("%d %d",&a,&b);
    for(c=1;c<=a && c<=b;c++){
    if(a%c==0 && b%c==0){
    hcf=c;
    }
    }
    printf("hcf is: %d",hcf);
    return 0;
}
