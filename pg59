// Online C compiler to run C program online
#include <stdio.h>

int main() {
 int a,b,c=0,sum=0,d,len,i,ip;
 printf("enter a number:");
 scanf("%d",&ip);
 a=ip;
 while (a>0)
 {  
     a=a/10;
     c=c+1;
 }
 printf("Length: %d\n",c);
 a=ip;
for(i=1;i<c;i++)
{
 d=a%10;
 sum=sum+d;
 a=a/10;
}
sum=sum+a;
printf("the sum of the digits is: %d",sum);
 
    return 0;
 
}
