// Online C compiler to run C program online
#include <stdio.h>

int main() {
    int x;
    printf("enter the number: ");
    scanf("%d",&x);
    if (x<=50)
    printf("1");
    else
    printf("0");
    return 0;
}
