// Online C compiler to run C program online
#include <stdio.h>
int disp_rsum();
int main() 
{
    disp_rsum();
    return(0);
}
int disp_rsum()
{
    int i,sum=0;
    for(i=6;i>=1;i--)
    sum=sum+i;
    printf("%d\n",sum);
    return(0);
}

  
