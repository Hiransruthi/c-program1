// Online C compiler to run C program online
#include <stdio.h>

int main() {
    int i,sum=0,a[5],b;
    printf("enter five numbers: ");
    for(i=1;i<6;i++)
    {
        scanf("%d",&a[i]);
    }
    for(i=1;i<6;i++)
    {
        sum=sum+a[i];
        
    }
    printf("sum= %d",sum);
    return 0;
}
