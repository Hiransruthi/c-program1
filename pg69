// Online C compiler to run C program online
#include <stdio.h>

int main() {
    int x,a,sum=0;
      //printf("enter a number:");
      //scanf("%d",&a);
    for(x=1;x<10;x++)
    {
        if(x%2!=0)
        printf(" %d\n",x);
      }
     

    return 0;
}
