// Online C compiler to run C program online
#include <stdio.h>

int main() {
     int x;
     printf("enter the num:");
     scanf("%d",&x);
    loop:if(x<6)
   { 
     printf("%d\n",x);
     x=x+1;
     goto loop;
   }
    return 0;
}  
