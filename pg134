// Online C compiler to run C program online
#include <stdio.h>
int count_total_digits();
int main() 
{
    count_total_digits();
    return(0);
}
int count_total_digits()
{
    int i,a,c=0,ip,b,d,sum=0;
    printf("enter the number: ");
    scanf("%d",&ip);
    a=ip;
    while(a>0)
    {
        i=a%10;
        c=c+1;
        a=a/10;
    }
    a=ip;
    for (b=1;b<=c;b++)
    {
        d=a%10;
        sum=sum+d;
        a=a/10;
    }
    printf("%d",sum);
    return(0);
}

  
