// Online C compiler to run C program online
#include <stdio.h>

int main() {
    int i,sum=0,a[5],large;
    printf("enter five numbers: ");
    for(i=1;i<6;i++)
    {
        scanf("%d",&a[i]);
    }
    large=a[0];
    for(i=1;i<5;i++)
    {
        if(a[i]>large)
        {
            large=a[i];
        }
        
    }
    printf("the largest number is : %d",large);
    return 0;
}
