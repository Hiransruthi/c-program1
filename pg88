#include<stdio.h>
#include<math.h>
int main() {
    int x,c=0,ip,count=0,i,a,n;
    printf("enter the num: ");
    scanf("%d",&ip);
    x=ip;
    while(x>0)
    {
       x=x/10;
       c=c+1;
    }
    //printf("count: %d\n",c);
    x=ip;
    for(i=1;i<=c;i++)
    {
        a=x%10;
        {
            for(n=0;n<=3;n++)
             {
                 if(a==pow(n,2))
                 count++;
             }
        }
        x=x/10;
    }
    printf("%d",count);
    return 0;
}
