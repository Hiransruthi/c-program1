// Online C compiler to run C program online
#include <stdio.h>

int main() {
 int x,a,b,c;
 for (x=10;x<100;x++)
 {
 if (x%2==0)
  {
   a=x%10;
   b=x/10;
   c=a+b;
  
 if (c==6)
  {
      printf("%d\n",x);
    }
  }
 }
    return 0;
 
}
