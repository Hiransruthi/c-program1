// Online C compiler to run C program online
#include <stdio.h>

int main() {
    int x;
      //printf("enter a number:");
      //scanf("%d",&a);
    for(x=10;x<20;x++)
    {
        if(x%2!=0)
        printf(" %d\n",x);
      }
     

    return 0;
}
