// Online C compiler to run C program online
#include <stdio.h>

int main() {
    int x,sum=0,a;
    printf("enter the number:");
    scanf("%d",&x);
    while(x/10>0 || x%10!=0)
    {
        a=x%10;
        sum=sum+a;
        x=x/10;
    }
    printf("%d\n",sum);
    return 0;
}
