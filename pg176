// Online C compiler to run C program online
#include <stdio.h>

int main() {
    int i,sum=0,a[5],b;
    float c;
    printf("enter five numbers: ");
    for(i=1;i<6;i++)
    {
        scanf("%d",&a[i]);
    }
    for(i=1;i<5;i++)
    {
        sum=sum+a[i];
        c=sum/5.0;
        
    }
    printf("sum= %.2f",c);
    return 0;
}
