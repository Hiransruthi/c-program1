// Online C compiler to run C program online
#include <stdio.h>

int count_total_digits();
int main()
{
    int x;
    x=count_total_digits();
    printf("the sum is %d",x);
    return 0;
}
int count_total_digits()
{
    int c=0,a,b,sum=0,d,ip,e;
    printf("enter the number: ");
    scanf("%d",&ip);
    a=ip;
    while(a>0)
    {
        b=a%10;
        c=c+1;
        a=a/10;
    }
    a=ip;
    for(d=1;d<=c;d++)
    {
       e=a%10;
       sum=sum+e;
       a=a/10;
    }
return sum;
}
//printf("%d is the sum",sum);
