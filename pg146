#include <stdio.h>
#include <math.h>

int disp_single_digit_square();

int main()
{
    //int ip;
    //printf("Enter a number: ");
    //scanf("%d", &ip);

    disp_single_digit_square();

    return 0;
}

int disp_single_digit_square()
{
    int i,a,c=0,b;
    printf("enter the number : ");
    scanf("%d",&i);
    while(i>0)
    {
        a=i%10;
        //printf("a=%d\n",a);
        for(b=1;b<4;b++)
        {
            if(a==pow(b,2))
             {
                c=c+1;
                //printf("%d",c);
             }
        }
        i=i/10;
    }
    printf("count=%d",c);
    return 0;
}
