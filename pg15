// Online C compiler to run C program online
#include <stdio.h>

int main() {
    int x,x1,x2;
    printf("enter the number: ");
    scanf("%d",&x);
    x1=x-5*(x%2);
    printf("answer: %d",x1);
    return 0;
}
