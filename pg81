// Online C compiler to run C program online
#include <stdio.h>

int main() {
    int x,a,flag=1;
    printf("enter the num: ");
    scanf("%d",&x);
    if(x<=1)
    {
        flag =0;
    }
    else
    {
        for (a=2;a<=x;a++)
        {
            if(x%a==0)
            {
                flag = 0;
                break;
            }
        }
    }
    if(flag == 1)
    printf("%d is  prime",x);
    else
    printf("%d is not prime",x);
   return 0;
}
